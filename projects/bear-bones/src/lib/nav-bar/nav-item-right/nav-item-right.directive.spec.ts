import { NavItemRightDirective } from './nav-item-right.directive';

describe('NavItemRightDirective', () => {
  it('should create an instance', () => {
    const directive = new NavItemRightDirective();
    expect(directive).toBeTruthy();
  });
});
