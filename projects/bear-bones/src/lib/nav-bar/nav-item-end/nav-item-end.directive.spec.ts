import { NavItemEndDirective } from './nav-item-end.directive';

describe('NavItemEndDirective', () => {
  it('should create an instance', () => {
    const directive = new NavItemEndDirective(null);
    expect(directive).toBeTruthy();
  });
});
