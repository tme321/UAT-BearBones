import { NavBarToggleDirective } from './nav-bar-toggle.directive';

describe('NavBarToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new NavBarToggleDirective();
    expect(directive).toBeTruthy();
  });
});
