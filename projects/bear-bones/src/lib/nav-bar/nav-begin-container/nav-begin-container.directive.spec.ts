import { NavBeginContainerDirective } from './nav-begin-container.directive';

describe('NavBeginContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new NavBeginContainerDirective();
    expect(directive).toBeTruthy();
  });
});
