import { NavEndContainerDirective } from './nav-end-container.directive';

describe('NavEndContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new NavEndContainerDirective(null);
    expect(directive).toBeTruthy();
  });
});
