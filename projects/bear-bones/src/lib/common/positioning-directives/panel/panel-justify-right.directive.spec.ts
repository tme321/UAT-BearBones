import { PanelJustifyRightDirective } from './panel-justify-right.directive';

describe('PanelJustifyRightDirective', () => {
  it('should create an instance', () => {
    const directive = new PanelJustifyRightDirective();
    expect(directive).toBeTruthy();
  });
});
