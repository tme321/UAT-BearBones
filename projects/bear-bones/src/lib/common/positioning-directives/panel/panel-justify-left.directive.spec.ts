import { PanelJustifyLeftDirective } from './panel-justify-left.directive';

describe('PanelJustifyLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new PanelJustifyLeftDirective();
    expect(directive).toBeTruthy();
  });
});
