import { PanelRightDirective } from './panel-right.directive';

describe('PanelRightDirective', () => {
  it('should create an instance', () => {
    const directive = new PanelRightDirective();
    expect(directive).toBeTruthy();
  });
});
