import { ToggleAfterPanelDirective } from './toggle-after-panel.directive';

describe('ToggleAfterPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleAfterPanelDirective();
    expect(directive).toBeTruthy();
  });
});
