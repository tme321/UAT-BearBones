import { ToggleBeforePanelDirective } from './toggle-before-panel.directive';

describe('ToggleBeforePanelDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleBeforePanelDirective();
    expect(directive).toBeTruthy();
  });
});
