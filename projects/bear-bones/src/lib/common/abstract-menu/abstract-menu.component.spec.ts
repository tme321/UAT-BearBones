import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AbstractMenuComponent } from './abstract-menu.component';
/*
describe('AbstractMenuComponent', () => {
  let component: AbstractMenuComponent;
  let fixture: ComponentFixture<AbstractMenuComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AbstractMenuComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AbstractMenuComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/