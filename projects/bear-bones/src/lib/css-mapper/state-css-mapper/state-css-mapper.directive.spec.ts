import { StateCssMapperDirective } from './state-css-mapper.directive';

describe('StateCssMapperDirective', () => {
  it('should create an instance', () => {
    const directive = new StateCssMapperDirective(null,null);
    expect(directive).toBeTruthy();
  });
});
