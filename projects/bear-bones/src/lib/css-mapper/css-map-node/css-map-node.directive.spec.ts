import { CssMapNodeDirective } from './css-map-node.directive';

describe('CssMapNodeDirective', () => {
  it('should create an instance', () => {
    const directive = new CssMapNodeDirective(null);
    expect(directive).toBeTruthy();
  });
});
