import { CssMapperDirective } from './css-mapper.directive';

describe('CssMapperDirective', () => {
  it('should create an instance', () => {
    const directive = new CssMapperDirective();
    expect(directive).toBeTruthy();
  });
});
