import { BBContentDirective } from './content.directive';

describe('BBContentDirective', () => {
  it('should create an instance', () => {
    const directive = new BBContentDirective(null);
    expect(directive).toBeTruthy();
  });
});
