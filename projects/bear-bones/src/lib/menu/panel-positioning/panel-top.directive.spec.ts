import { PanelTopDirective } from './panel-top.directive';

describe('MenuTopDirective', () => {
  it('should create an instance', () => {
    const directive = new PanelTopDirective();
    expect(directive).toBeTruthy();
  });
});
