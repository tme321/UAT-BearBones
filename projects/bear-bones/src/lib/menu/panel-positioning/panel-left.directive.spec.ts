import { PanelLeftDirective } from './panel-left.directive';

describe('PanelLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new PanelLeftDirective();
    expect(directive).toBeTruthy();
  });
});
