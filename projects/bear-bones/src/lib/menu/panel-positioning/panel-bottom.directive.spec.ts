import { PanelBottomDirective } from './panel-bottom.directive';

describe('PanelBottomDirective', () => {
  it('should create an instance', () => {
    const directive = new PanelBottomDirective();
    expect(directive).toBeTruthy();
  });
});
