import { BBDraggableDirective } from './draggable.directive';

describe('BBDraggableDirective', () => {
  it('should create an instance', () => {
    const directive = new BBDraggableDirective(null,null);
    expect(directive).toBeTruthy();
  });
});
