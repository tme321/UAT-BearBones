import { BBDragAndDropContainerComponent } from './drag-and-drop-container.component';

describe('BBDragAndDropContainerComponent', () => {
  it('should create an instance', () => {
    const directive = new BBDragAndDropContainerComponent(null,null);
    expect(directive).toBeTruthy();
  });
});
