import { BBToggleDirective } from './toggle.directive';

describe('ToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new BBToggleDirective();
    expect(directive).toBeTruthy();
  });
});
