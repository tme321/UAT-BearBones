import { BBContentContainerDirective } from './content-container.directive';

describe('BBContentContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new BBContentContainerDirective(null,null);
    expect(directive).toBeTruthy();
  });
});
