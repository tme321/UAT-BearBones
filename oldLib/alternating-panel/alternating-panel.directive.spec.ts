import { AlternatingPanelDirective } from './alternating-panel.directive';

describe('AlternatingPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new AlternatingPanelDirective();
    expect(directive).toBeTruthy();
  });
});
